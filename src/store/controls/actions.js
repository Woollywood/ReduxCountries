export const setSearch = (search) => ({
	type: 'setSearch',
	payload: search,
});

export const setRegion = (region) => ({
	type: 'setRegion',
	payload: region,
});

export const resetControls = {
	type: 'resetControls',
};
